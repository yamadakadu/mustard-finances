# Copy to .env and fill in your values. .env is gitignored.

DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=movimentacoes

# JSON list of allowed CORS origins (Next.js dev server, etc.)
CORS_ORIGINS=["http://localhost:3000"]
